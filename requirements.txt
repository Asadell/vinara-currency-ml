# Rupiah Vision - Training Pipeline (revisi)
#
# Catatan versi:
#   - TensorFlow 2.16+ pakai Keras 3. Kode ini ditulis untuk Keras 3
#     (pakai model.export() untuk SavedModel, bukan model.save(folder)).
#   - albumentations DIPIN ke 2.0.x. Kode ini memakai API baru (`fill`,
#     `fill_mask`, `num_holes_range`, `std_range`, `interpolation_pair`).
#     Batas ">=1.4.15" yang lama itu keliru: di 1.4.15 argumennya masih
#     `value`/`mask_value`, jadi kalau pip kebetulan memilih versi itu SEMUA
#     transform melempar exception, augmentasi mati diam-diam, dan training
#     tetap jalan mulus tanpa pesan apa pun. Diverifikasi jalan di 2.0.8.
#   - albumentations sendiri cuma butuh numpy>=1.24.4 (tanpa batas atas),
#     jadi pin numpy di bawah ini tidak bentrok.

tensorflow>=2.16.0,<2.20.0
numpy>=1.24.0,<2.1.0
opencv-python-headless>=4.8.0
albumentations>=2.0.0,<2.1.0
matplotlib>=3.7.0
Pillow>=10.0.0

# Opsional: cuma dipakai kalau kamu mau menarik dataset dari Roboflow
roboflow>=1.1.0
